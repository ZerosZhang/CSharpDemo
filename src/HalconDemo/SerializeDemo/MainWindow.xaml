<Window x:Class="SerializeDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SerializeDemo — Halcon HObject Json 序列化"
        Height="560" Width="900"
        FontFamily="Consolas, 微软雅黑" FontSize="16" >

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Width" Value="120" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Margin" Value="10" />
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center">
            <Button x:Name="Button_Problem1" Content="问题1 复现" Click="Button_Problem1_Click" />
            <Button x:Name="Button_Solution1" Content="问题1 解决" Click="Button_Solution1_Click" />
            <Button x:Name="Button_Problem2" Content="问题2 复现" Click="Button_Problem2_Click" />
            <Button x:Name="Button_Solution2" Content="问题2 解决" Click="Button_Solution2_Click" />
        </StackPanel>

        <Border Grid.Column="1">
            <TextBox x:Name="TextBox_Log" IsReadOnly="True" Padding="10" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" />
        </Border>
    </Grid>
</Window>
